import 'dart:ui';

class ButtonColor {
  static const first = Color(0xFF027D9A); // Teal-Blue
  static const second = Color(0xFF038C7F); // Blue-Green
  static const third = Color(0xFF04A27A); // Soft Green
  static const shadow = Color(0x33027D9A); // Transparent teal for shadow
  static const skip = Color(0xFFCCEAE6); // Slightly darker than E0F7F4
  // Pale greenish background
  static const alert = Color.fromRGBO(244, 67, 54, 1);
}

class Constants {
  static const logoImage = "assets/images/logo.png";
}

class ColorConst {
  static const gradientStart = Color(0xFFF1FBFC);
  static const gradientEnd = Color(0xFFF5FBF4);

  static const cursorDarkGreen = Color(
    0xFF00695C,
  ); // Dark Teal-Green (for cursor)
  static const textDarkGreen = Color(0xFF004D40); // Deeper Green (for text)
}
